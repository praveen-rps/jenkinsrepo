pipeline {
    agent any

    stages {
        stage('Integrate') {
            steps {
                echo 'Integration stage is done'
            }
        }
	stage('Build') {
            steps {
                echo 'Build stage is done'
            }
        }
	stage('Deploy') {
            steps {
                echo 'Deployment stage is done'
            }
        }
    }
}